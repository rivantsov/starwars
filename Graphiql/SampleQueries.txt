query starshipsAndCharacters {
  
  starships{name, length}

  characters(episode: JEDI) {name}
}

query searchQuery {
  search(text: "d") {__typename, name}
}


query introspectionQueries {
  __type(name: "Droid") { 
  	fields {
      name
      type { displayName }
    }
  }

  # all type names
  __schema {types {name}}
}
